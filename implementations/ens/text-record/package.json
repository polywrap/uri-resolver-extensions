{
  "name": "ens-text-record-uri-resolver-wrap",
  "description": "Wrapper resolving ENS text records",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "codegen": "npx polywrap codegen",
    "build": "npx polywrap build",
    "deploy": "npx polywrap deploy -o deployment.json",
    "test": "jest --passWithNoTests --runInBand --verbose"
  },
  "devDependencies": {
    "ethereum-provider-js": "0.2.1",
    "@types/jest": "27.0.3",
    "jest": "26.6.3",
    "polywrap": "0.11.0-pre.4",
    "ts-jest": "26.5.4",
    "typescript": "4.0.7"
  }
}
