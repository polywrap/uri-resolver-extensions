{
  "name": "ens-ocr-contenthash-uri-resolver-wrap",
  "description": "ENS OCR Contenthash URI Resolver",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "npx polywrap build",
    "codegen": "npx polywrap codegen",
    "deploy": "npx polywrap deploy -o deployment.json",
    "test": "jest --passWithNoTests --runInBand --verbose"
  },
  "devDependencies": {
    "@polywrap/client-js": "0.11.0",
    "@types/jest": "27.0.3",
    "jest": "26.6.3",
    "polywrap": "0.11.0-pre.4",
    "ts-jest": "26.5.4",
    "typescript": "4.1.6"
  }
}
